Thinking1	XGBoost与GBDT的区别是什么？
Answer1    GBDT：
                 1、利用损失函数的负梯度方向在当前模型的值作为残差的近似值，进而拟合一棵CART回归树
                 2、在优化的时候只用到一阶导数信息
                 3、在相对较少的调参时间下，预测的准确度较高
                 4、不能并行训练数据
                 5、它能灵活的处理各种类型的数据
           xGBoost：
                 1、除了支持CART树以外还支持线性分类器，这个时候XGBoost相当于L1和L2正则化的逻辑斯蒂回归（分类）或者线性回归（回归）
                 2、对损失函数进行了二阶泰勒展开，得到一阶和二阶导数
                 3、在损失函数中把树模型复杂度作为正则项加到优化目标中，降低了模型的方差，防止过拟合
                 4、借鉴了随机森林的做法，支持列抽样，不仅防止过拟合，还能减少计算量
                 5、对于特征的值有缺失的样本，可以自动学习出它的分裂方向
                 6、在特征上可以支持并行处理（数据事先排序并且以block形式存储，有利于并行计算）
                 7、在寻找最佳分割点时，采用近似贪心算法，用来加速计算


Thinking1	举一个你之前做过的预测例子（用的什么模型，解决什么问题，比如我用LR模型，对员工离职进行了预测，效果如何... 请分享到课程微信群中）
Answer2    用Xgboost模型，对员工离职进行了预测，在kaggle上的评分为0.80487
           1、padas读取csv数据，删除无意义的变量，并检查剩余字段是否含有缺失值
           2、将数值型的字段转换为类别型，方便对其哑变量处理
           3、根据皮尔逊系数确定特征字段与标签字段的系数关系，把不相关字段去除
           4、将数据集拆分为训练集和测试集，方便后续算法调用
           5、分别用GridSearchCV结合LR、决策树、Xgboost、RF算法算出该算法的最优参数并计算评分（最终选用Xgboost算法）

Thinking2	请你思考，在你的工作中，需要构建哪些特征（比如用户画像，item特征...），这些特征都包括哪些维度（鼓励分享到微信群中，进行交流）
Answer3    在工作中接触过用户画像，是基于公司内部员工的数据平台，特征有员工身份、入职年限、工作国家、工作城市、管理者、internet权限、email权限、系统标识、操作频率等


